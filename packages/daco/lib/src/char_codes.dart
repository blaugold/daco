/// Character code for a space character.
const int $SPACE = 32;

/// Character code for a bullet ('&#x2022;') character.
const int $BULLET = 0x2022;

/// String which contains the [$BULLET] character.
final bullet = String.fromCharCode($BULLET);
